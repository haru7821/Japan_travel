# Google Analytics 4 (GA4) 설정 가이드

이 사이트는 완전한 정적 사이트라 지금까지는 방문자가 얼마나 오는지,
어느 도시·어느 섹션을 많이 보는지 전혀 알 방법이 없었습니다. 이
가이드를 따라 **GA4**(구글의 무료 분석 도구)를 연결하면 실시간
방문자 수, 인기 지역/섹션, 유입 경로 등을 확인할 수 있습니다.

**설정하지 않아도** 사이트는 지금처럼 완전히 정상 작동합니다. GA4
스크립트는 설정 전까지 아예 로드되지 않습니다.

---

## 1단계 — GA4 속성 만들기 (5분, 무료)

1. [https://analytics.google.com](https://analytics.google.com) 접속 후 구글 계정으로 로그인
2. "측정 시작" → 계정 이름 입력(예: `일본 여행 가이드`) → 다음
3. 속성 이름 입력(예: `japan-travel-guide`) → 시간대는 `대한민국` 또는 `일본` → 통화는 `KRW` 또는 `JPY` → 다음
4. 비즈니스 정보는 대충 채우고 "만들기" → 이용약관 동의
5. 플랫폼 선택에서 **웹** 선택
6. 웹사이트 URL에 `https://haru7821.github.io/Japan_travel/` 입력, 스트림 이름은 아무거나
7. "스트림 만들기"를 누르면 **측정 ID**(`G-`로 시작하는 문자열, 예: `G-ABC1234XYZ`)가 나옵니다. 이 값을 복사해두세요.

---

## 2단계 — 이 저장소에 측정 ID 넣기

`index.html`에서 `GA4_MEASUREMENT_ID_PLACEHOLDER` 문자열을 검색해
1단계에서 복사한 실제 측정 ID로 바꿉니다.

```js
var GA4_MEASUREMENT_ID = 'GA4_MEASUREMENT_ID_PLACEHOLDER'; // ← 여기를 'G-ABC1234XYZ' 처럼 실제 값으로
```

저장 후 `git commit` → `git push`. 반영되면 자동으로 추적이
시작됩니다(버튼이나 별도 설정 없음).

---

## 3단계 — 확인

1. 배포된 사이트(`haru7821.github.io/Japan_travel/`)에 접속
2. GA4 관리 화면 왼쪽 메뉴에서 **보고서 → 실시간** 클릭
3. 사이트에서 지역 탭을 바꾸거나(후쿠오카→오사카) 다른 메뉴(골프, 테마투어 등)를 눌러보고, 실시간 보고서에 "조회수"가 올라가는지 확인
4. 며칠 지나면 **보고서 → 참여도 → 페이지 및 화면**에서 어느 지역/섹션이 많이 보이는지 확인 가능

---

## 참고 — SPA(단일 페이지) 방식이라 조금 다릅니다

이 사이트는 지역 탭이나 메뉴를 눌러도 실제로 페이지가 새로 로드되지
않습니다(URL 해시만 바뀜: `#/osaka/food` 같은 식). 그래서 GA4의
자동 페이지뷰 집계 방식으로는 이런 화면 전환을 못 잡습니다. 이미
코드에서 이 부분을 처리해뒀습니다 — 지역이나 메뉴가 바뀔 때마다
`#/지역/화면` 경로를 GA4에 직접 전송하도록 만들어져 있어서,
설정만 하면 각 지역·섹션별 조회수가 정상적으로 잡힙니다.

## 참고 — 클로드 아티팩트에서는 왜 카운트가 안 되나요?

이 사이트는 GitHub Pages(라이브 사이트)와 claude.ai 아티팩트(미리보기)
두 곳에 동시에 배포됩니다. 아티팩트는 보안 정책상 외부 스크립트(구글
태그 매니저 등)를 전면 차단하는데, GA4는 구글 서버에서 스크립트를
불러와야 하므로 **아티팩트 미리보기에서는 추적 기능이 자동으로
비활성화**됩니다(에러 없이 조용히 숨김 처리). 실제 배포 사이트에서만
방문자가 집계됩니다.

## 참고 — 비용 & 개인정보

GA4는 이 정도 트래픽에서는 **완전 무료**입니다. 기본 설정 그대로는
쿠키 배너 없이도 EU 밖에서는 일반적으로 문제없이 쓰이지만, 방문자가
많아지거나 유럽 방문자 비중이 높아지면 쿠키 동의 배너 추가를 고려해
볼 수 있습니다(현재는 별도로 넣지 않았습니다).
